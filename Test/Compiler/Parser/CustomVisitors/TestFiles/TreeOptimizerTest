function main()
     /*create DiGraph XYZ = testGraph end

     //Omskrives til
     create DiGraph XYZ end
     XYZ = testGraph
     */

     real j = 0.3
     create DiGraph testGraph
         a -- {b[0.3],c}
         b -- d[j]
     end

     //OMSKRIVES TIL:
     /*create DiGraph testGraph end
     Vertex a
     Vertex b
     Vertex c
     Vertex d
     testGraph.addVertex(a)
     testGraph.addVertex(b)
     testGraph.addVertex(c)
     testGraph.addVertex(d)
     testGraph.addEdge(a,b)
     testGraph.addEdge(a,c)
     testGraph.addEdge(b,d)
     testGraph.getEdge(a,b).weight = 0.3
     testGraph.getEdge(a,c).weight = 1.0
     testGraph.getEdge(b,d).weight = 1.0
     */

      /*for i = 0 to 10 do
        int a
      end*/


      /*create Queue<real> Q
            7.8
            3.2
      end*/
      //Skal omskrives til
      /*create Queue<real> Q end
      Q.enqueue(7.8)
      Q.enqueue(3.2)
      */
      //s = stackOfStackCreate().pop().x
end

